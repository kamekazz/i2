export const acModular = (prVal) => {
    let payload = prVal
    return {
        type: 'ADDMODULER',
        payload: payload
    };
}

export const acModularAsment = (prVal) => {
    let payload = prVal
    return {
        type: 'ASMENT',
        payload: payload
    };
}

